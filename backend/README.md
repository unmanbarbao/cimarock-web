# Backend

Espacio reservado para la API y CMS de CimaRock (ediciones, artistas, convocatorias, journal, galerías, aliados, documentos).

Los mockups actuales viven en `frontend/` con datos JSON estáticos que anticipan este modelo.
