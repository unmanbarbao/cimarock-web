# Netlify — mockup CimaRock
# Base directory en UI: frontend (o dejar vacío si el build usa este archivo desde raíz)

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
